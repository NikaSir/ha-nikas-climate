# NikaS Climate

Автономная специализированная панель Home Assistant для локального управления кондиционерами NikaS.

## Назначение

Панель использует локальные сущности `Syncleo (Rusclimate/Polaris) WiFi (UDP)` и не должна зависеть от облачного управления кондиционерами.

Стартовая конфигурация:

- `11.2 - Гостиная` — кондиционер в зале;
- `14 - Веранда` — кондиционер на веранде;
- внешний контроль температуры — комнатные Zigbee-датчики, переданные в Syncleo как `Virtual Sensor`.

## Архитектурный принцип температуры

1. Контрольная температура помещения — внешний датчик HA / Virtual Sensor Syncleo.
2. Температура внутреннего блока — служебное диагностическое значение.
3. Уставка — целевая температура `climate`-сущности кондиционера.
4. Связь — локальный UDP/LAN; отсутствие WAN у Wi-Fi-донгла считается штатным режимом.

## Сводка

Первая страница показывает основную температуру помещения, уставку, температуру внутреннего блока и полный текущий режим работы: HVAC-режим, скорость вентилятора, жалюзи, Night, Turbo и разницу между комнатным и встроенным датчиками. Эталонная двухуровневая плашка связи NikaS/S8 OMNI отображается только на этой странице.

## Управление

Команды не отправляются при каждом нажатии. Изменения режима и уставки сначала формируют целевое состояние в панели, после чего отправляются одной кнопкой **«Применить»**.

- текущее состояние — голубая подсветка;
- выбранное целевое состояние — зелёная подсветка;
- после подтверждённого обновления Home Assistant целевое состояние становится текущим;
- кнопки режимов используют информативную MDI-иконку сверху и подпись снизу.

## Интеграция

- Domain: `nikas_climate`
- Integration: `NikaS Climate`
- Version: `0.1.3`
- UI: `v1.1.2`
- Route: `/dashboard-climate-v1/home`

## Установка

Добавить репозиторий в HACS как пользовательскую интеграцию, установить `NikaS Climate`, перезапустить Home Assistant и добавить интеграцию через **Настройки → Устройства и службы**.

## Источник управления

Панель не заменяет Syncleo. Syncleo отвечает за локальный протокол кондиционеров; NikaS Climate предоставляет отдельный эксплуатационный интерфейс и связывает кондиционер с контрольным датчиком помещения.
